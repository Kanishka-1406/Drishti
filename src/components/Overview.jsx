import React from 'react';import RiskRing from './RiskRing.jsx';import ContributionBars from './ContributionBars.jsx';import {RECOMMENDATIONS} from '../utils/riskEngine.js'
export default function Overview({region,risk,setTab}){return <><div className="page-head"><div><div className="eyebrow">LIVE PROTOTYPE • REGIONAL VIEW</div><h1>Risk overview</h1><p>Explainable disaster-risk intelligence for landslide scenario exploration.</p></div><button className="primary" onClick={()=>setTab('simulator')}>Run scenario →</button></div><div className="hero-grid"><section className="panel score-panel"><div><div className="panel-label">ESTIMATED RISK</div><RiskRing score={risk.score} tier={risk.tier}/><div className={`tier ${risk.tier}`}>{risk.tier}</div></div><div className="region-copy"><div className="panel-label">SELECTED REGION</div><h2>{region.name}</h2><p>{region.state}</p><div className="explain">{risk.explanation}</div><div className="mini-grid"><span>Rainfall<strong>{region.rainfall} mm</strong></span><span>Slope<strong>{region.slope}°</strong></span><span>Soil saturation<strong>{region.soil}%</strong></span><span>Historical<strong>{region.historical}</strong></span></div></div></section><section className="panel"><div className="panel-label">RISK DRIVERS</div><h3>What is driving the estimate?</h3><ContributionBars contributions={risk.contributions}/><div className="recommend"><b>Preparedness guidance</b><p>{RECOMMENDATIONS[risk.tier]}</p></div></section></div></>}
