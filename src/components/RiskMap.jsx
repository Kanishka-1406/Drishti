import React,{useEffect} from 'react';import {MapContainer,TileLayer,CircleMarker,Popup,useMap} from 'react-leaflet';import 'leaflet/dist/leaflet.css';import {REGIONS} from '../data/regions.js';import {computeRisk,tierColor} from '../utils/riskEngine.js'
function Fly({region}){const m=useMap();useEffect(()=>m.flyTo([region.lat,region.lng],6,{duration:.6}),[region,m]);return null}
export default function RiskMap({selectedId,setSelectedId}){const selected=REGIONS.find(r=>r.id===selectedId)||REGIONS[0];return <><div className="page-head"><div><div className="eyebrow">REGIONAL MONITORING</div><h1>Risk map</h1><p>Demonstration regions across Northeast India.</p></div><div className="legend"><span>● Low</span><span>● Moderate</span><span>● High</span><span>● Critical</span></div></div><section className="map-panel"><MapContainer center={[26.2,91.5]} zoom={5} scrollWheelZoom><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><Fly region={selected}/>{REGIONS.map(r=>{const x=computeRisk(r);return <CircleMarker key={r.id} center={[r.lat,r.lng]} radius={10} pathOptions={{color:tierColor(x.tier),fillColor:tierColor(x.tier),fillOpacity:.85}} eventHandlers={{click:()=>setSelectedId(r.id)}}><Popup><b>{r.name}</b><br/>{r.state}<br/><strong>{x.score}/100 • {x.tier}</strong><br/><small>Simulation input</small></Popup></CircleMarker>})}</MapContainer><div className="map-note">Demo locations and values are simulation inputs — not official hazard boundaries.</div></section></>}
