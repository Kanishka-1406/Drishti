import React from 'react';import {REGIONS} from '../data/regions.js';import {computeRisk,tierColor} from '../utils/riskEngine.js';import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,CartesianGrid} from 'recharts'
export default function Insights(){const data=REGIONS.map(r=>({...r,score:computeRisk(r).score,tier:computeRisk(r).tier}));const avg=Math.round(data.reduce((a,b)=>a+b.score,0)/data.length),high=data.filter(x=>x.score>=60).length,top=[...data].sort((a,b)=>b.score-a.score)[0];return <><div className="page-head"><div><div className="eyebrow">PORTFOLIO VIEW</div><h1>Insights</h1><p>Regional comparison from the same explainable scoring engine.</p></div></div><div className="stats"><div className="stat"><small>MONITORED</small><b>{data.length}</b><span>demo regions</span></div><div className="stat"><small>AVERAGE RISK</small><b>{avg}</b><span>out of 100</span></div><div className="stat"><small>HIGH / CRITICAL</small><b>{high}</b><span>regions</span></div><div className="stat"><small>HIGHEST</small><b>{top.score}</b><span>{top.name}</span></div></div><section className="panel chart"><div className="panel-label">REGIONAL RISK COMPARISON</div><ResponsiveContainer width="100%" height={310}><BarChart data={data} margin={{top:15,right:10,left:0,bottom:35}}><CartesianGrid strokeDasharray="3 3" stroke="#21332e"/><XAxis dataKey="name" angle={-18} textAnchor="end" height={55} stroke="#8ea99f"/><YAxis domain={[0,100]} stroke="#8ea99f"/><Tooltip contentStyle={{background:'#0f1c19',border:'1px solid #2d443c',color:'#eaf1ee'}}/><Bar dataKey="score" fill="#3fd8c4" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></section></>}
