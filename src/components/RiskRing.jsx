import React from 'react';import {tierColor} from '../utils/riskEngine.js'
export default function RiskRing({score,tier,size=170}){const stroke=10,r=(size-stroke)/2,c=2*Math.PI*r,color=tierColor(tier);return <div className="risk-ring-wrap" style={{width:size,height:size}}><svg width={size} height={size} style={{transform:'rotate(-90deg)'}}><circle cx={size/2} cy={size/2} r={r} fill="none" stroke="var(--border)" strokeWidth={stroke}/><circle cx={size/2} cy={size/2} r={r} fill="none" stroke={color} strokeWidth={stroke} strokeDasharray={c} strokeDashoffset={c-(score/100)*c} strokeLinecap="round"/></svg><div className="ring-center"><b style={{color}}>{score}</b><span>/ 100</span></div></div>}
